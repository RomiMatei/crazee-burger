import Login from './components/Login';

function App() {
  // state(état, données)

  // comportements

  // affichage(render)

  return (
    <div>
      <Login />
    </div>
  );
}

export default App;
